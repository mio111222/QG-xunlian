链式队列完成了基本的功能，并且数据存储上采用void*型，可支持整型，字符，浮点型和字符串的存储，通过修改第三行的MAXSIZE可以修改队列的长度length=n-1
当创建队列，选择输入整型和浮点型，但是输入是字符时，会默认为0，避免程序卡死




四则运算则可以进行带负数，小数，括号前省略*号的运算，如-3+(-5)+3(2+3)/5=-5
但是无法运行两个连续()且省略*号的计算（但是如果带上*号，则可以进行两个连续()的计算）